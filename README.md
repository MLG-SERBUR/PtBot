1. Install python
    - Windows: `py` doesn't install properly from any other source unless you run the python installer as admin. https://www.python.org/ftp/python/3.13.8/python-3.13.8-amd64.exe
    - Python really garbage yet ppl use this and react so what else is new https://stackoverflow.com/questions/53319416/python-3-6-installation-failed
    - Linux: May also need `sudo apt install python3.10-venv` or something
2. setup.sh or setup.bat
3. `run.bat <dumcord bot token>` or `run.sh <dumcord bot token>`
4. Join voice channel and send `!join`
5. Kick bot with `!leave`
